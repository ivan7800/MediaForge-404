{
  "name": "mediaforge-404",
  "private": true,
  "version": "4.1.0",
  "type": "module",
  "description": "Suite multimedia privada con U404 Universal Media Engine, Google Cast, AirPlay y Cast Bridge local.",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/finalize-sw.mjs",
    "preview": "vite preview",
    "prepare:vendor": "node scripts/prepare-vendor.mjs",
    "prebuild": "npm run prepare:vendor",
    "test": "npm run build && playwright test",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report",
    "test:smoke": "npm run build && node scripts/smoke-inline.mjs"
  },
  "dependencies": {
    "@ffmpeg/ffmpeg": "0.12.15",
    "@ffmpeg/util": "0.12.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "vite": "^7.3.6"
  }
}
